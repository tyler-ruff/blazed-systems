import { FooterLinks, Menu } from "~/utils/config";

const navMenu = {
    _id: "base",
    nav: [
        {
            label: "Home",
            href: "/"
        }
    ],
} as Menu;

export {
    navMenu
};